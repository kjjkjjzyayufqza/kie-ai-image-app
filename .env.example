# Exact public origin used to reject cross-origin Key proxy requests.
APP_ORIGIN=http://localhost:3000

# Comma-separated exact Kie result hosts verified by contract tests.
# Defaults already include tempfile.aiquickdraw.com, tempfile.redpandaai.co,
# kieai.redpandaai.co, and static.aiquickdraw.com.
NEXT_PUBLIC_KIE_ASSET_HOSTS=
